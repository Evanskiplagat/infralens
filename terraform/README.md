# terraform

Reserved for the read-only IAM role/policy module referenced in [docs/security.md](../docs/security.md), scoped to exactly the permissions `infralens scan` calls. Not yet implemented — see [docs/roadmap.md](../docs/roadmap.md).

State files, `.terraform/`, and overrides are gitignored; never commit them.
