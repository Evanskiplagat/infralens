export function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="eyebrow">Deprecated</p>
        <h1>InfraLens</h1>
        <p className="lead">
          InfraLens is now a CLI, not a web app. This shell is kept only as
          reference for a possible future optional local viewer — see the
          repository&apos;s root README and docs/roadmap.md.
        </p>
      </section>
    </main>
  );
}

